# МІНІСТЕРСТВО ОСВІТИ І НАУКИ УКРАЇНИ
# НАЦІОНАЛЬНИЙ УНІВЕРСИТЕТ «ОСТРОЗЬКА АКАДЕМІЯ»
## Кафедра інформаційних технологій та аналітики даних

<br>

# ЗВІТ
# ПРО ПРОХОДЖЕННЯ ПРАКТИКИ ВИРОБНИЧОЇ ПРОФЕСІЙНО-ОРІЄНТОВАНОЇ

здобувача (ки) вищої освіти  
першого (бакалаврського) рівня  
четвертого року навчання групи КН-42  
спеціальності 122 Комп'ютерні науки  
ОПП «Комп'ютерні науки»  

**Тарасюка Дмитра Олександровича**

<br>

**Термін практики** з 26.01.2026 по 20.02.2026 р.  
**База практики** — ТОВ «ХАНІКОМБ СОФТ»

<br>

**Керівник практики від університету**  
викладач-стажист **Денис МАЦЕВИЧ**

<br>

Члени комісії: ______________________  
_______________________  
_______________________

<br>

**Острог — 2026 р.**

---

# Зміст

ВСТУП .................................................................................................................... 3  
ПРЕДМЕТНЕ СЕРЕДОВИЩЕ ................................................................................. 4  
3.1 Опис системи та користувачі ...................................................................... 4  
3.2 Функціональна модель та процес діяльності ............................................. 5  
АРХІТЕКТУРА РІШЕННЯ ...................................................................................... 6  
4.1 Загальна архітектура (Next.js App Router) .............................................. 6  
4.2 Маршрутизація, layout-и та захист ............................................................ 7  
4.3 Сервісний шар та інтеграція з API ........................................................... 8  
РОЗДІЛ 1. ВИВЧЕННЯ NEXT.JS ......................................................................... 9  
1.1 Вступ ......................................................................................................... 9  
1.2 React Foundations .................................................................................... 9  
1.3 App Router ................................................................................................ 10  
1.4 Pages Router .............................................................................................. 10  
1.5 SEO ............................................................................................................. 11  
РОЗДІЛ 2. ПЕРЕНОС ПРОЕКТУ OaEdu НА NEXT.JS ......................................... 12  
2.1 Вступ ......................................................................................................... 12  
2.2 Створення проекту (Next.js, структура папок, конфіг) ............................ 12  
2.3 Інфраструктура (layout, API, HttpClient, авторизація) ............................ 14  
2.4 Layout-и та ProtectedRoute ..................................................................... 15  
2.5 Login та волонтерство (список, деталі) ................................................. 16  
2.6 Новини (категорії та динамічна сторінка) .............................................. 17  
2.7 Головна та розклад (захищені сторінки) ................................................ 18  
2.8 AdminPanel та всі підсторінки ................................................................. 18  
2.9 SEO (metadata, robots, sitemap) та виправлення ..................................... 19  
2.10 Перевірка та тестування ......................................................................... 20  
ВИСНОВОК ......................................................................................................... 21  
СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ ................................................................. 22  
ГРАФІК ПРОХОДЖЕННЯ ПРАКТИКИ .............................................................. 23  

---

# ВСТУП

Під час практики я зосередився на вивченні фреймворку Next.js та переносі існуючого веб-проекту OaEdu з React (Vite) на Next.js з використанням App Router. Цей напрямок актуальний для сучасної веб-розробки, оскільки Next.js надає серверний рендеринг, покращене SEO, файлову маршрутизацію та інтеграцію з сучасним стеком React.

Метою практики було: ознайомитися з офіційним курсом Learn Next.js на платформі [nextjs.org/learn](https://nextjs.org/learn), засвоїти основи React Foundations, App Router, Pages Router та SEO; виконати повний перенос проекту OaEdu (освітня платформа Острозької академії) у новий проект oaedunextjs зі збереженням функціональності, захисту маршрутів та додаванням SEO-оптимізації.

Результатом стало повністю робочий проект на Next.js 16 з перенесеними сторінками (головна, розклад, новини, волонтерство, адмін-панель), авторизацією, layout-ами та налаштованими metadata, sitemap та robots для пошукових систем.

---

# ПРЕДМЕТНЕ СЕРЕДОВИЩЕ

## 3.1 Опис системи та користувачі

**OaEdu** — веб-застосунок освітньої платформи Національного університету «Острозька академія». Система призначена для інформаційної підтримки навчального процесу: перегляд розкладу занять, отримання новин університету, ознайомлення з можливостями волонтерства та (для адміністраторів) управління довідниками та контентом через адмін-панель.

**Користувачі системи:**

- **Здобувач (студент, викладач)** — авторизований користувач, який переглядає головну сторінку, розклад занять, новини; має доступ до особистого кабінету після входу (Login). Не має доступу до адмін-панелі.
- **Адміністратор** — користувач з роллю «Admin», який крім перегляду має доступ до розділу AdminPanel: управління розкладами, викладачами, предметами, спеціальностями, ролями, кафедрами, групами, типами занять, номерами пар, новинами та волонтерством.

**Публічні можливості (без авторизації):** перегляд сторінки входу (Login), розділу «Новини» (категорії та статті), розділу «Волонтерство» (список та деталі). Захищені розділи (головна, розклад) та AdminPanel потребують авторизації; AdminPanel — додатково перевірки ролі.

## 3.2 Функціональна модель та процес діяльності

**Основні функціональні підсистеми:**

1. **Автентифікація** — вхід за логіном/паролем, збереження JWT-токена, перевірка прав доступу при переході на захищені маршрути.
2. **Інформаційні сторінки** — головна (HomePage), розклад (schedule з фільтрами за групою/викладачем/датою), новини (категорії: загальні, спорт, події, наука, культура; сторінка деталей новини), волонтерство (список та деталі оголошень).
3. **Адміністрування** — CRUD для сутностей: розклади, викладачі, предмети, спеціальності, ролі, кафедри, групи, типи занять, номери пар, новини, волонтерство. Кожен підрозділ має таблицю, форму додавання/редагування, пошук за ID або іншими полями.

**Процес діяльності (спрощено):** користувач відкриває сайт → переглядає публічні розділи (новини, волонтерство) або переходить на вхід → після авторизації отримує доступ до головної та розкладу; адміністратор додатково отримує доступ до AdminPanel і виконує операції з довідниками та контентом. Усі дані для відображення та змін надходять з зовнішнього API (бекенд); фронтенд (oaedunextjs) відповідає за інтерфейс, маршрутизацію, захист маршрутів та виклики API.

---

# АРХІТЕКТУРА РІШЕННЯ

## 4.1 Загальна архітектура (Next.js App Router)

Рішення **oaedunextjs** побудоване на **Next.js 16** з використанням **App Router**. Це означає файлову маршрутизацію: кожен каталог у `src/app/` з файлом `page.tsx` відповідає URL-шляху; `layout.tsx` у папці визначає спільний layout для усіх дочірніх маршрутів. Група маршрутів `(main)` не додає сегмента до URL, але дозволяє спільний layout (MainLayout) для головної, розкладу, новин, волонтерства та адмін-панелі.

**Рівні архітектури:**

- **Клієнт (браузер)** — отримує HTML/CSS/JS, виконує клієнтські компоненти (наприклад, форми, ProtectedRoute, інтерактивні списки).
- **Next.js (сервер + клієнт)** — серверний рендеринг кореневого HTML, генерація metadata для SEO, обслуговування статики; клієнтська навігація після першого завантаження (SPA-подібна поведінка в межах App Router).
- **Зовнішній API (бекенд)** — автентифікація, отримання розкладів, новин, волонтерства, довідників; всі запити йдуть через один базовий URL (наприклад, `NEXT_PUBLIC_API_URL`), з додаванням заголовка `Authorization: Bearer <token>` та при необхідності `X-User-Id`.

**Стек:** React 19, TypeScript, MUI 7 (Material-UI) з Material-NextJS для сумісності з App Router, Tailwind CSS, axios для HTTP, jwt-decode для перевірки токена.

## 4.2 Маршрутизація, layout-и та захист

- **Кореневий layout** (`app/layout.tsx`) — задає мову сторінки (uk), глобальні метадані (title, description, openGraph), обгортає додаток у провайдери: AppRouterCacheProvider (MUI), AuthProvider (стан авторизації), ThemeProvider (світла/темна тема).
- **Вкладені layout-и:** `(main)/layout.tsx` повертає `MainLayout` (Header + Footer); усередині `(main)` окремі гілки можуть мати власний контекст (наприклад, `news/layout.tsx` з NewsLayout і metadata «Новини», `AdminPanel/layout.tsx` з AdminLayout і ProtectedRoute role="Admin").
- **Захист маршрутів** реалізований патерном **ProtectedRoute**: клієнтський компонент перевіряє `isAuthenticated` та (опційно) роль користувача; при невиконанні умов виконується редірект на `/Login`. Захищені сторінки (головна, розклад) та layout AdminPanel обгорнуті в `<ProtectedRoute>` або `<ProtectedRoute role="Admin">`.

Таким чином, архітектура маршрутів комбінує файлову структуру Next.js з явною перевіркою прав на клієнті, без дублювання логіки на сервері Next.js (middleware для перевірки ролей у поточній реалізації не використовується).

## 4.3 Сервісний шар та інтеграція з API

Клієнт не викликає API напряму з компонентів; використовується **сервісний шар**: клас `HttpClient` (axios) з базовим URL, таймаутом та інтерцепторами. Інтерцептори автоматично додають до запитів заголовок `Authorization` з токеном з `localStorage` та (за наявності) `X-User-Id`; при відповіді 401 токен видаляється і користувач перенаправляється на Login. Окремі сервіси (AuthService, NewsService, ScheduleService, TeacherService тощо) створують екземпляр `HttpClient` і експортують методи `get`, `post`, `put`, `delete` для відповідних ендпоінтів. Компоненти викликають ці сервіси (наприклад, `ScheduleService.getAll()`, `AuthService.login()`), що забезпечує єдину точку конфігурації та обробки помилок авторизації.

---

# РОЗДІЛ 1. ВИВЧЕННЯ NEXT.JS

## 1.1 Вступ

Під час практики я використовував офіційний інтерактивний курс **Learn Next.js** на платформі [nextjs.org/learn](https://nextjs.org/learn) [1]. Курс охоплює перехід від основ React до побудови повноцінного застосунку на Next.js: від налаштування середовища та стилізації до роботи з даними, маршрутизації, SEO та розгортання. Навчання було поділено на блоки: React Foundations, App Router, Pages Router та SEO.

## 1.2 React Foundations

**Мета завдання:**  
Ознайомитися з базовими концепціями React, необхідними для роботи з Next.js: компоненти, JSX, пропси, стан (useState), події, умовний рендеринг та списки. Розділ допомагає тим, хто переходить з чистого JavaScript або інших фреймворків, і закладає основи для розуміння серверних та клієнтських компонентів у Next.js.

**Виконано:** вивчення розділу React Foundations на nextjs.org/learn.

*Рис. 1.* Приклад проходження розділу React Foundations на nextjs.org/learn *(тут можна вставити скріншот інтерфейсу курсу або виконаного завдання).*

## 1.3 App Router

**Мета завдання:**  
Освоїти файлову маршрутизацію Next.js (App Router): структуру папки `app`, файли `layout.tsx`, `page.tsx`, динамічні сегменти `[slug]`, вкладені layout-и, навігацію за допомогою компонента `<Link>`, а також відмінності між Server Components та Client Components. App Router є рекомендованим підходом у Next.js для нових проектів і підтримує streaming, layouts та покращене завантаження.

**Виконано:** вивчення розділу App Router на nextjs.org/learn.

## 1.4 Pages Router

**Мета завдання:**  
Ознайомитися з класичним маршрутизатором Next.js (Pages Router): створення статичного блогу з використанням файлів `pages/index.js`, `pages/[slug].js`, getStaticPaths та getStaticProps. Розуміння Pages Router потрібне для підтримки існуючих проектів та порівняння з App Router.

**Виконано:** вивчення розділу Next.js Pages Router на nextjs.org/learn.

## 1.5 SEO

**Мета завдання:**  
Навчитися додавати метадані (metadata) до Next.js-застосунку: title, description, openGraph, генерація metadata для окремих сторінок та динамічних маршрутів. Ознайомитися з можливостями покращення індексації пошуковими системами та підготовки контенту для соціальних мереж.

**Виконано:** вивчення розділу Adding Metadata (SEO) на nextjs.org/learn.

---

# РОЗДІЛ 2. ПЕРЕНОС ПРОЕКТУ OaEdu НА NEXT.JS

## 2.1 Вступ

У цьому розділі виконано повний перенос веб-застосунку OaEdu (React, Vite, React Router) у новий проект **oaedunextjs** на базі Next.js 16 з використанням App Router. Вихідний проект містив сторінки: вхід (Login), головна (HomePage), розклад (schedule), новини з категоріями та деталями, волонтерство (список та деталі), адмін-панель з 11 підрозділами (розклади, викладачі, предмети, спеціальності, ролі, кафедри, групи, типи занять, номери пар, новини, волонтерство). Захист маршрутів реалізовано через компонент ProtectedRoute та перевірку ролі Admin. Під час переносу збережено всю функціональність, перенесено API-сервіси (HttpClient, Auth, User, News, Schedule тощо), layout-и та додано SEO (metadata, robots, sitemap).

## 2.2 Створення проекту (Next.js, структура папок, конфіг)

**Мета:**  
Ініціалізувати новий проект Next.js (create-next-app), налаштувати TypeScript, Tailwind CSS, структуру папок `src/app`, `src/components`, `src/features`, `src/api`, а також конфігурацію для MUI (Material-UI) та Material-NextJS. Забезпечити базові env-змінні для API.

**Виконано:** створено проект oaedunextjs з Next.js 16, React 19, налаштовано package.json, next.config, Tailwind, MUI 7 та App Router. Структура проекту:

```
oaedunextjs/
├── public/                    # статичні файли, зображення, іконки
├── src/
│   ├── app/                   # App Router — маршрути та layout-и Next.js
│   │   ├── layout.tsx         # кореневий layout (html, провайдери, metadata)
│   │   ├── globals.css        # глобальні стилі
│   │   ├── not-found.tsx      # сторінка 404
│   │   ├── robots.ts, sitemap.ts   # SEO
│   │   ├── (main)/            # група маршрутів з спільним MainLayout
│   │   │   ├── layout.tsx, page.tsx   # головна /
│   │   │   ├── HomePage/       # /HomePage
│   │   │   ├── schedule/      # /schedule
│   │   │   ├── news/          # /news, категорії, [category]/[slug]
│   │   │   ├── volunteering/  # /volunteering, /volunteering/[id]
│   │   │   └── AdminPanel/    # /AdminPanel + підсторінки
│   │   └── Login/             # /Login
│   ├── components/            # переиспользуемые: layouts, auth, icons, common
│   ├── features/              # Auth, HomePage, SchedulePage, NewsPage, VolunteeringPage, AdminPage
│   ├── api/services/         # HttpClient, AuthService, UserService, NewsService, ScheduleService, …
│   ├── providers/             # AuthContext, ThemeProvider
│   ├── hooks/, types/, lib/, utils/
├── package.json               # Next 16, React 19, MUI 7, Tailwind, axios, jwt-decode
├── next.config.js, tsconfig.json, tailwind.config, postcss.config
```

**Коротко:** `app/` визначає URL-и (файлова маршрутизація); `components/` — спільні layout-и та UI; `features/` — логіка сторінок (новини, розклад, адмінка тощо); `api/services/` — запити до бекенду; `providers/` — контексти авторизації та теми.

**Рішення для Next.js.** Щоб запити з клієнта йшли на той самий хост (уникнення CORS) та імпорт SVG як компонентів працював, у проект додано `next.config.ts`: rewrites перенаправляють `/api/*` на бекенд; webpack-правило для SVG підключає @svgr/webpack.

*Лістинг 2.А.* Конфігурація Next.js — rewrites для API та webpack для SVG (додано при переносі, `next.config.ts`).

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  async rewrites() {
    const backendUrl = process.env.API_BACKEND_URL || "http://localhost:8000";
    return [{ source: "/api/:path*", destination: `${backendUrl}/:path*` }];
  },
  webpack(config) {
    config.module.rules.push({
      test: /\.svg$/i,
      issuer: /\.[jt]sx?$/,
      use: ["@svgr/webpack"],
    });
    return config;
  },
};
export default nextConfig;
```

*Рис. 2.* Структура проекту oaedunextjs або запуск застосунку *(скріншот папок src/app, src/components або терміналу після npm run dev).*

## 2.3 Інфраструктура (layout, API, HttpClient, авторизація)

**Мета:**  
Реалізувати кореневий `layout.tsx` (html, lang="uk", провайдери), підключити `globals.css`, створити HttpClient для запитів до бекенду та перенести API-сервіси (AuthService, UserService). Налаштувати AuthContext та провайдер авторизації для збереження стану користувача та токена.

**Реалізація.** Кореневий layout задає мову сторінки, глобальні метадані для SEO та композицію провайдерів (MUI cache, авторизація, тема). HttpClient централізує базовий URL, таймаут та інтерцептори: на кожен запит додаються заголовки Authorization та X-User-Id; при 401 токен видаляється і виконується редірект на Login. Сервіси (Auth, User, News, Schedule тощо) використовують один екземпляр HttpClient і експортують методи get/post/put/delete.

**Виконано:** root layout з metadata та провайдерами (AppRouterCacheProvider, AuthProvider, ThemeProvider); перенесено HttpClient та сервіси Auth, User, News, Schedule, Teacher, Subject, Group, Department, Role, ClassType, ClassNumber, Specialty, Volunteering, VolunteeringCategory.

*Лістинг 1.* Кореневий layout — metadata та провайдери (`src/app/layout.tsx`).

```tsx
import type { Metadata } from "next";
import "./globals.css";
import { AppRouterCacheProvider } from "@mui/material-nextjs/v16-appRouter";
import { AuthProvider } from "@/providers/AuthContext";
import { ThemeProvider } from "@/providers/theme/ThemeProvider";

export const metadata: Metadata = {
  title: {
    default: "OaEdu – Освітня платформа Острозької академії",
    template: "%s | OaEdu",
  },
  description: "Онлайн-платформа Острозької академії: розклад занять, новини...",
  openGraph: { title: "OaEdu – ...", description: "...", locale: "uk_UA", type: "website" },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="uk">
      <body>
        <AppRouterCacheProvider>
          <AuthProvider>
            <ThemeProvider>{children}</ThemeProvider>
          </AuthProvider>
        </AppRouterCacheProvider>
      </body>
    </html>
  );
}
```

*Лістинг 2.* HttpClient — інтерцептори та методи get/post (`src/api/services/HttpClient.ts`, фрагмент).

```ts
private initInterceptors() {
  this.axiosInstance.interceptors.request.use((config) => {
    if (typeof window !== "undefined") {
      const token = localStorage.getItem("token");
      if (token) config.headers["Authorization"] = `Bearer ${token}`;
      const userId = getUserIdFromToken();
      if (userId) config.headers["X-User-Id"] = userId;
    }
    return config;
  }, (error) => Promise.reject(error));

  this.axiosInstance.interceptors.response.use(
    (response) => response,
    (error) => {
      if (typeof window !== "undefined" && error?.response?.status === 401) {
        localStorage.removeItem("token");
        window.location.href = "/Login";
      }
      return Promise.reject(error);
    }
  );
}

public async get<T>(url: string, config?: AxiosRequestConfig): Promise<T> {
  return this.axiosInstance.get(url, config).then((r) => r.data as T);
}
public async post<T, D = unknown>(url: string, data: D, config?: AxiosRequestConfig): Promise<T> {
  return this.axiosInstance.post(url, data, config).then((r) => r.data as T);
}
```

## 2.4 Layout-и та ProtectedRoute

**Мета:**  
Перенести layout-и з OaEdu: MainLayout (Header + Footer), NewsLayout (з категоріями новин), AdminLayout (меню адмін-панелі). Реалізувати компонент ProtectedRoute для захисту сторінок (перевірка авторизації та опційно ролі Admin). Підключити (main)/layout.tsx до MainLayout.

**Реалізація.** ProtectedRoute — клієнтський компонент ("use client"): отримує контекст авторизації та дані поточного користувача; якщо користувач не авторизований або не має потрібної ролі (наприклад, "Admin"), виконується редірект на `/Login`; під час завантаження показується Loader. Сторінки, що потребують захисту, обгортаються в `<ProtectedRoute>` або `<ProtectedRoute role="Admin">`. Layout (main) лише підключає MainLayout (Header + Footer), без логіки захисту — захист реалізований на рівні окремих page.tsx або layout AdminPanel.

**Виконано:** MainLayout, MainLayoutHeader, MainLayoutFooter; NewsLayout та NewsLayoutHeader; AdminLayout та AdminLayoutHeader; ProtectedRoute з підтримкою role="Admin"; (main)/layout.tsx обгортає контент у MainLayout.

*Лістинг 3.* ProtectedRoute — перевірка авторизації та ролі, редірект (`src/components/auth/ProtectedRoute.tsx`).

```tsx
"use client";
import { useRouter } from "next/navigation";
import { useAuth } from "@/providers/AuthContext";
import { useCurrentUser } from "@/hooks/useCurrentUser";

const hasRole = (userRole: string, role?: string | string[]) => {
  if (!role) return true;
  if (Array.isArray(role)) return role.includes(userRole);
  return userRole === role;
};

const ProtectedRoute: React.FC<{ role?: string | string[]; children?: React.ReactNode; redirectTo?: string }> = ({
  role, children, redirectTo = "/Login",
}) => {
  const router = useRouter();
  const { isAuthenticated } = useAuth();
  const { user, loading } = useCurrentUser();
  const shouldRedirect = !loading && (!isAuthenticated || !user || !hasRole(user?.role_name ?? "", role));

  useEffect(() => {
    if (shouldRedirect) router.push(redirectTo);
  }, [shouldRedirect, redirectTo, router]);

  if (loading) return <Loader />;
  if (shouldRedirect) return null;
  return <>{children}</>;
};
```

*Лістинг 4.* Головна сторінка — використання ProtectedRoute (`src/app/(main)/page.tsx`).

```tsx
import HomePage from "@/features/HomePage/HomePage";
import ProtectedRoute from "@/components/auth/ProtectedRoute";

export default function RootPage() {
  return (
    <ProtectedRoute>
      <HomePage />
    </ProtectedRoute>
  );
}
```

*Лістинг 5.* Layout секції (main) — композиція MainLayout (`src/app/(main)/layout.tsx`).

```tsx
import MainLayout from "@/components/layouts/MainLayout";

export default function MainSectionLayout({ children }: { children: React.ReactNode }) {
  return <MainLayout>{children}</MainLayout>;
}
```

## 2.5 Login та волонтерство (список, деталі)

**Мета:**  
Перенести сторінку входу `/Login` (layout + page) та публічні маршрути `/volunteering` та `/volunteering/[id]`. Адаптувати компоненти Login, VolunteeringPage та VolunteeringDetails до Next.js (Link замість NavLink, useParams для id).

**Особливості реалізації.** Сторінка Login розташована в `app/Login/` і не обгортається в (main), тому має мінімальний layout без MainLayout. Для волонтерства використано динамічний сегмент `[id]`: у компоненті деталей оголошення `id` береться з `useParams()` замість React Router; посилання формуються через компонент `Link` з next/link з href `/volunteering/${id}`.

**Виконано:** реалізовано `/Login`, `/volunteering`, `/volunteering/[id]`; перенесено компоненти з OaEdu з необхідними змінами імпортів та навігації.

**Рішення.** У Next.js параметр динамічного сегмента `[id]` отримують через `useParams()` з `next/navigation` (замість `useParams()` з react-router-dom). Сторінка-обгортка лише рендерить фічу; сама фіча VolunteeringDetails читає `params.id` і за нього знаходить оголошення.

*Лістинг 5.А.* Сторінка деталей волонтерства та фрагмент фічі — useParams з next/navigation (oaedunextjs).

Маршрут `app/(main)/volunteering/[id]/page.tsx`:

```tsx
"use client";
import VolunteeringDetails from "@/features/VolunteeringPage/VolunteeringDetails";

export default function VolunteeringDetailsRoute() {
  return <VolunteeringDetails />;
}
```

У `VolunteeringDetails.tsx` — отримання id з URL:

```tsx
import { useParams, useRouter } from "next/navigation";

const VolunteeringDetails: React.FC = () => {
  const params = useParams<{ id: string }>();
  const router = useRouter();
  // ...
  useEffect(() => {
    if (params?.id) {
      const item = dataSource.find((x) => x.id === params.id) || null;
      setData(item);
    }
  }, [params?.id]);
  // ...
};
```

*Рис. 3.* Сторінка входу або розділ «Волонтерство» *(скріншот екрану Login або списку волонтерства).*

## 2.6 Новини (категорії та динамічна сторінка)

**Мета:**  
Перенести розділ новин: layout для `/news`, сторінки `/news`, `/news/general`, `/news/sport`, `/news/events`, `/news/science`, `/news/culture` та динамічний маршрут `/news/[category]/[slug]` для деталей новини. Перенести компоненти GeneralNews, SportNews, EventsNews, ScienceNews, CultureNews та NewsDetails з підтримкою params та searchParams у Next.js.

**Реалізація.** Кожна категорія новин має власну папку під `app/(main)/news/` з `page.tsx`. Деталі новини обслуговуються одним динамічним маршрутом `news/[category]/[slug]/page.tsx`: компонент отримує `params` (category, slug) і за потреби викликає API для отримання статті за slug; дані більше не передаються через location.state, як у React Router, що відповідає ідіоматичному підходу Next.js до динамічних сторінок.

**Виконано:** реалізовано всі маршрути новин; NewsDetails адаптовано до отримання даних з URL/params замість location.state.

**Рішення.** Деталі новини обслуговуються динамічним маршрутом `news/[category]/[slug]/page.tsx`; компонент NewsDetails отримує дані через пропси або `useSearchParams()` з `next/navigation` (замість `location.state` з React Router), що дозволяє коректно відкривати статтю за посиланням.

*Лістинг 6.А.* Динамічний маршрут деталей новини та фрагмент NewsDetails — useSearchParams (oaedunextjs).

`app/(main)/news/[category]/[slug]/page.tsx`:

```tsx
"use client";
import NewsDetails from "@/features/NewsPage/NewsDetails";

export default function NewsDetailsRoute() {
  return <NewsDetails />;
}
```

У `NewsDetails.tsx` — дані з URL (пропси або searchParams):

```tsx
import { useRouter, useSearchParams } from "next/navigation";

const NewsDetails: React.FC<NewsDetailsProps> = (props) => {
  const params = useSearchParams();
  const title = props.title ?? params.get("title") ?? undefined;
  const content = props.content ?? params.get("content") ?? undefined;
  // ... якщо !title — router.back()
};
```

*Рис. 4.* Розділ «Новини» або сторінка деталей новини *(скріншот категорій новин або окремої статті).*

## 2.7 Головна та розклад (захищені сторінки)

**Мета:**  
Перенести захищені сторінки: головну `/` та `/HomePage`, розклад `/schedule`. Обгорнути їх у ProtectedRoute. Перенести компоненти HomePage, SchedulePage та допоміжні (ScheduleTable, ScheduleFilters, ScheduleNavigation, ScheduleDays тощо).

**Рішення.** Кожна захищена сторінка — окремий `page.tsx`, який обгортає фічу в `<ProtectedRoute>`; логіка однакова для головної та розкладу. Розклад не експортує серверні дані; сторінка клієнтська за потреби (SchedulePage сам робить запити).

**Виконано:** (main)/page.tsx та /HomePage/page.tsx відображають HomePage з ProtectedRoute; /schedule — SchedulePage з ProtectedRoute; перенесено всі пов’язані компоненти.

*Лістинг 7.А.* Сторінка розкладу — захист через ProtectedRoute (oaedunextjs, `src/app/(main)/schedule/page.tsx`).

```tsx
import SchedulePage from "@/features/SchedulePage/SchedulePage";
import ProtectedRoute from "@/components/auth/ProtectedRoute";

export default function ScheduleRoute() {
  return (
    <ProtectedRoute>
      <SchedulePage />
    </ProtectedRoute>
  );
}
```

## 2.8 AdminPanel та всі підсторінки

**Мета:**  
Перенести адмін-панель: layout `/AdminPanel` з ProtectedRoute role="Admin", сторінки SchedulesAdmin, TeacherAdmin, SubjectAdmin, SpecialtyAdmin, RoleAdmin, DepartmentAdmin, GroupAdmin, ClassTypeAdmin, ClassNumberAdmin, NewsAdmin, VolunteeringAdmin. Зберегти інтеграцію з відповідними API-сервісами та useEntityDetails.

**Процес реалізації.** Layout AdminPanel обгортає контент у AdminLayout (меню з посиланнями на підсторінки) та в `<ProtectedRoute role="Admin">`, тому доступ мають лише авторизовані користувачі з роллю Admin. Кожен підрозділ (наприклад, SchedulesAdmin, NewsAdmin) розміщено в окремій папці з `page.tsx`; компоненти повторно використовують спільний хук useEntityDetails для відображення деталей сутностей у діалозі та той самий набір API-сервісів, що й у вихідному OaEdu.

**Виконано:** реалізовано AdminPanel layout та усі 11 підсторінок у структурі app/(main)/AdminPanel/... з окремими page.tsx; перенесено компоненти з OaEdu.

**Рішення.** Доступ до всієї адмін-панелі захищається одним layout: `AdminPanel/layout.tsx` обгортає контент у `<ProtectedRoute role="Admin">` і в AdminLayout (меню). Усі підсторінки автоматично захищені без дублювання перевірки в кожному page.

*Лістинг 8.А.* Layout адмін-панелі — захист за роллю Admin (oaedunextjs, `src/app/(main)/AdminPanel/layout.tsx`).

```tsx
"use client";

import React from "react";
import AdminLayout from "@/components/layouts/AdminLayout";
import ProtectedRoute from "@/components/auth/ProtectedRoute";

export default function AdminSectionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <ProtectedRoute role="Admin">
      <AdminLayout>{children}</AdminLayout>
    </ProtectedRoute>
  );
}
```

*Рис. 5.* Адмін-панель проекту oaedunextjs *(скріншот меню адмінки або підсторінки, наприклад Розклади або Новини).*

## 2.9 SEO (metadata, robots, sitemap) та виправлення

**Мета:**  
Додати глобальні metadata в root layout (title, description, openGraph, locale); metadata для окремих розділів (Новини, Волонтерство, Вхід). Створити `robots.ts` (allow/disallow, sitemap) та `sitemap.ts` з основними URL. Виконати виправлення з плану перевірки міграції (ProtectedRoute для `/`, GeneralNews, NewsDetails, Login).

**Реалізація.** У вихідному OaEdu (Vite + React) не було вбудованої підтримки metadata, robots.txt та sitemap; це додано саме в oaedunextjs за рахунок можливостей Next.js App Router. Нижче наведено код, який було додано при переносі: файли `robots.ts` та `sitemap.ts` у `src/app/` (Next.js автоматично обслуговує маршрути `/robots.txt` та `/sitemap.xml`), а також приклад metadata для окремого розділу (Login).

**Виконано:** у layout.tsx задано title (template "%s | OaEdu"), description, openGraph; у відповідних layout — metadata для Новини, Волонтерство, Login; додано robots.ts та sitemap.ts; виправлено відображення контенту згідно MIGRATION_VERIFICATION_PLAN.

*Лістинг 6.* Файл robots — правила для пошукових роботів (додано в oaedunextjs, `src/app/robots.ts`).

```ts
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: ["/", "/HomePage", "/schedule", "/news", "/volunteering"],
      disallow: ["/admin", "/AdminPanel"],
    },
    sitemap: "https://example.com/sitemap.xml",
  };
}
```

*Лістинг 7.* Файл sitemap — перелік URL для індексації (додано в oaedunextjs, `src/app/sitemap.ts`).

```ts
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = "https://example.com";

  return [
    { url: `${baseUrl}/`, lastModified: new Date(), changeFrequency: "daily", priority: 1 },
    { url: `${baseUrl}/HomePage`, lastModified: new Date(), changeFrequency: "weekly", priority: 0.9 },
    { url: `${baseUrl}/schedule`, lastModified: new Date(), changeFrequency: "daily", priority: 0.9 },
    { url: `${baseUrl}/news`, lastModified: new Date(), changeFrequency: "hourly", priority: 0.8 },
    { url: `${baseUrl}/volunteering`, lastModified: new Date(), changeFrequency: "weekly", priority: 0.7 },
    { url: `${baseUrl}/Login`, lastModified: new Date(), changeFrequency: "yearly", priority: 0.3 },
  ];
}
```

*Лістинг 8.* Metadata для розділу «Вхід» — приклад доданого для окремого маршруту (oaedunextjs, `src/app/Login/layout.tsx`).

```tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Вхід",
  description: "Увійдіть до освітньої платформи OaEdu Острозької академії.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

## 2.10 Перевірка та тестування

**Мета:**  
Перевірити відповідність усіх маршрутів OaEdu та oaedunextjs, роботу захисту та ролей, коректність відображення сторінок. Підготувати документування (README, план перевірки міграції, звіт).

**Рішення.** У Next.js App Router сторінка 404 реалізується через файл `not-found.tsx` в корені `app/`. Додано кастомну сторінку з useRouter з `next/navigation` та кнопкою повернення — при неіснуючому маршруті користувач бачить стилізовану 404 замість стандартної.

**Виконано:** порівняно маршрути, перевірено ProtectedRoute та AdminPanel; оновлено/створено документацію та звіт; додано `not-found.tsx` для обробки 404.

*Лістинг 10.А.* Кастомна сторінка 404 — not-found.tsx (oaedunextjs, `src/app/not-found.tsx`, фрагмент).

```tsx
"use client";

import React from "react";
import { Box, Typography, Button } from "@mui/material";
import { useRouter } from "next/navigation";

export default function NotFound() {
  const router = useRouter();
  return (
    <Box sx={{ width: "100%", height: "100vh", display: "flex", alignItems: "center", justifyContent: "center", ... }}>
      <Typography variant="h4">Сторінку не знайдено</Typography>
      <Button onClick={() => router.push("/")}>На головну</Button>
    </Box>
  );
}
```

---

# ВИСНОВОК

Під час проходження практики я отримав досвід у вивченні Next.js за офіційним курсом Learn Next.js та повному переносі веб-застосунку OaEdu з React (Vite) на Next.js.

У звіті описані предметне середовище (система OaEdu, користувачі, функціональна модель та процес діяльності) та архітектура рішення oaedunextjs (App Router, маршрутизація, layout-и, захист маршрутів, сервісний шар API). Листинги коду ілюструють кореневий layout, HttpClient з інтерцепторами, компонент ProtectedRoute та приклад захищеної сторінки.

У Розділі 1 я ознайомився з платформою [nextjs.org/learn](https://nextjs.org/learn): основами React (React Foundations), файловою маршрутизацією (App Router), класичним Pages Router та додаванням метаданих для SEO. Це заклало основу для подальшої роботи над проектом на Next.js.

У Розділі 2 виконано повний перенос проекту: створено структуру Next.js-проекту, перенесено інфраструктуру (layout, API, авторизацію), layout-и та ProtectedRoute, усі публічні та захищені сторінки (Login, волонтерство, новини, головна, розклад), адмін-панель з усіма підсторінками, а також налаштовано SEO (metadata, robots, sitemap) та виправлення згідно з планом перевірки. Роутер повністю відповідає старому проекту; захист маршрутів та роль Admin збережені.

Практика дозволила закріпити навички роботи з Next.js, App Router, TypeScript, MUI та інтеграції з бекенд-API, а також підготувати проект до подальшого розгортання та підтримки.

---

# СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ

1. Learn Next.js. (n.d.). Start building with Next.js. Retrieved from https://nextjs.org/learn  
2. Next.js Documentation. (2026). App Router, Routing, Metadata. Retrieved from https://nextjs.org/docs  
3. React Documentation. (2026). React Docs. Retrieved from https://react.dev  
4. Vercel. (2026). Next.js by Vercel. Retrieved from https://nextjs.org  
5. MUI Material-UI. (2026). Material UI for React. Retrieved from https://mui.com/material-ui  
6. OaEdu (React, Vite) — вихідний проект репозиторію OaEduUa_React.

---

# ГРАФІК ПРОХОДЖЕННЯ ПРАКТИКИ ВИРОБНИЧОЇ ПРОФЕСІЙНО-ОРІЄНТОВАНОЇ

здобувача 4 курсу першого (бакалаврського) рівня вищої освіти  
освітньої програми «Комп'ютерні науки» група КН-42  

**Тарасюк Дмитро**

*(власне ім'я та прізвище)*

| № з/п | Дата | Назви робіт | Відмітки про виконання |
|-------|------|-------------|------------------------|
| 1 | 2 | 3 | 4 |
| 1 | 26.01.2026 – 08.02.2026 | https://nextjs.org/learn | Виконано |
| 2 | 26.01.2026 – 28.01.2026 | React Foundations | Виконано |
| 3 | 29.01.2026 – 31.01.2026 | App Router | Виконано |
| 4 | 01.02.2026 – 03.02.2026 | Pages Router | Виконано |
| 5 | 04.02.2026 – 08.02.2026 | SEO | Виконано |
| 6 | 09.02.2026 | Створив проект (Next.js, структура папок, конфіг) | Виконано |
| 7 | 10.02.2026 – 11.02.2026 | Інфраструктура (layout, API, HttpClient, авторизація) | Виконано |
| 8 | 12.02.2026 | Layout-и та ProtectedRoute | Виконано |
| 9 | 13.02.2026 | Login та волонтерство (список, деталі) | Виконано |
| 10 | 14.02.2026 | Новини (категорії та динамічна сторінка) | Виконано |
| 11 | 15.02.2026 | Головна та розклад (захищені сторінки) | Виконано |
| 12 | 16.02.2026 | AdminPanel та всі підсторінки | Виконано |
| 13 | 17.02.2026 | SEO (metadata, robots, sitemap) та виправлення | Виконано |
| 14 | 17.02.2026 – 18.02.2026 | Перевірка та тестування | Виконано |
| 15 | 18.02.2026 | Написання звіту | Виконано |

<br>

Керівник від бази практики _____________________ **Денис МАЦЕВИЧ**  
М.П. (підпис) (власне ім'я та прізвище)
